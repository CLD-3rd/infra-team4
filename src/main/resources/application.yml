spring:
  application:
    name: room-reservation
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/studyroom?serverTimezone=Asia/Seoul
    username: root
    password: cloud

  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

  security:
    user:
      name: admin
      password: admin

server:
  port: 8080
